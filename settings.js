const user = JSON.parse(localStorage.getItem("streakup_user") || "null");
if (!user) window.location.replace("/login");
const profile = JSON.parse(localStorage.getItem("streakup_profile") || "null") || { name: user.name || "Usuário", username: (user.email || "usuario").split("@")[0], avatarUrl: "" };
const avatarPreview = document.getElementById("avatarPreview");
const nameInput = document.getElementById("displayName");
const usernameInput = document.getElementById("username");
const avatarInput = document.getElementById("avatarUrl");
const setAvatar = () => { avatarPreview.textContent = profile.name.charAt(0).toUpperCase(); if (profile.avatarUrl) { avatarPreview.textContent = ""; avatarPreview.style.backgroundImage = `url(${profile.avatarUrl})`; avatarPreview.style.backgroundSize = "cover"; avatarPreview.style.backgroundPosition = "center"; } };
nameInput.value = profile.name; usernameInput.value = profile.username; avatarInput.value = profile.avatarUrl; document.getElementById("profileName").textContent = profile.name; document.getElementById("profileUsername").textContent = `@${profile.username}`; setAvatar();
document.getElementById("profileForm").addEventListener("submit", async (event) => { event.preventDefault(); const status = document.getElementById("profileStatus"); profile.name = nameInput.value.trim(); profile.username = usernameInput.value.trim().replace(/^@/, "").replace(/[^a-zA-Z0-9_.]/g, ""); profile.avatarUrl = avatarInput.value.trim(); status.className = ""; status.textContent = "Salvando..."; try { const response = await fetch("/api/update-profile", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: user.email, name: profile.name, username: profile.username, avatarUrl: profile.avatarUrl }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); user.name = profile.name; user.username = profile.username; user.avatarUrl = profile.avatarUrl; localStorage.setItem("streakup_user", JSON.stringify(user)); localStorage.setItem("streakup_profile", JSON.stringify(profile)); document.getElementById("profileName").textContent = profile.name; document.getElementById("profileUsername").textContent = `@${profile.username}`; setAvatar(); status.className = "success"; status.textContent = "Perfil atualizado."; } catch (error) { status.className = "error"; status.textContent = error.message || "Não foi possível atualizar."; } });
document.getElementById("passwordForm").addEventListener("submit", async (event) => { event.preventDefault(); const status = document.getElementById("passwordStatus"); status.textContent = "Atualizando..."; try { const response = await fetch("/api/change-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: user.email, currentPassword: document.getElementById("currentPassword").value, newPassword: document.getElementById("newPassword").value }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); status.className = "success"; status.textContent = "Senha atualizada."; event.target.reset(); } catch (error) { status.className = "error"; status.textContent = error.message || "Não foi possível atualizar."; } });
document.getElementById("logoutButton").addEventListener("click", () => { localStorage.removeItem("streakup_user"); window.location.replace("/login"); });
